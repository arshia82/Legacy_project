# FILE: myfita/apps/backend/programs/api/urls.py

from django.urls import path

# Placeholder views - add real views later (e.g., ProgramListView)
# from .views import ProgramListView, ProgramDetailView  # Uncomment when views exist

app_name = "programs"

urlpatterns = [
    # Core program endpoints (aligned with Business Plan: "program purchase delivery (PDF)")
    # path("", ProgramListView.as_view(), name="program-list"),  # List all programs
    # path("<uuid:pk>/", ProgramDetailView.as_view(), name="program-detail"),  # Program details
    
    # Add more as needed - this is minimal to unblock
]